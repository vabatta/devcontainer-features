{
	"id": "surrealdb",
	"name": "SurrealDB CLI",
	"version": "1.0.0",
	"description": "Installs SurrealDB CLI.",
	"options": {
		"version": {
			"type": "string",
			"proposals": [
				"latest"
			],
			"default": "latest",
			"description": "A specific version of SurrealDB to install. (e.g. \"1.0.0\")"
		}
	},
	"installsAfter": [
		"ghcr.io/devcontainers/features/common-utils"
	]
}